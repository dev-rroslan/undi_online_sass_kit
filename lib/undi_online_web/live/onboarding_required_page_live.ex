defmodule UndiOnlineWeb.OnboardingRequiredPageLive do
  @moduledoc false
  use UndiOnlineWeb, :live_view

  def render(assigns) do
    ~H"""
    Just some example content that is here to see when you have completed the onboarding.
    """
  end
end